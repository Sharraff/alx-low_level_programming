#include "main.h"
/**
 * _islower - this function checks for lowercase characters
 *
 * Return: Always 0
 *
 *@c: character to compare
 */
int _islower(int c)
{
	char i;
	int lower;

	if (c >= 'a' && c <= 'z')
	{
		if (i == c)
			lower = 1;
	}
	return (lower);
}

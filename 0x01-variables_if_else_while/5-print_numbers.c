#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - entry point
 *
 * Return: Always return 0 (Success)
 *
 */
int main(void)
{
	int a = 0;

	for (a = 0; a < 0 ; a++)
		printf("%d", a);
	putchar('\n');
	return (0);
}

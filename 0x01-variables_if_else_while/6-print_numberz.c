#include <stdio.h>
#include <stdio.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always return 0 (Success)
 *
 */
int main(void)
{
	int d = 48;

	while (d < 58)
	{
		putchar(d);
		d++;
	}
	putchar('\n');
	return (0);
}
